{
  "name": "pyr",
  "version": "0.1.1",
  "exports": "./lib.ts",
  "exclude": ["site"],
  "fmt": {
    "lineWidth": 100,
    "exclude": ["site"]
  },
  "lint": {
    "exclude": ["site"],
    "rules": {
      "tags": ["recommended"]
    }
  },
  "tasks": {
    "test": "deno test -A"
  }
}
